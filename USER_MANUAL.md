# AemeathTool 功能概览与使用手册

## 1. 产品定位

AemeathTool 是一个基于 Electron 的轻量浮窗网页/视频播放器，核心场景是把 B 站或其他常用网页放在桌面上方观看、操作或临时查阅。

它目前更接近“紧凑型悬浮浏览器”，不是完整浏览器。重点功能包括：

- 窗口置顶、半透明、鼠标穿透
- B 站视频页纯净播放和选集模式
- 网页登录状态持久保存
- 历史、收藏、保存页侧栏
- 可自定义工具栏按钮和全局快捷键
- 一键清理当前站点或全部网页数据

## 2. 当前主要功能

### 2.1 浮窗窗口

- 自定义无边框窗口。
- 支持拖动窗口、最小化、最大化/还原、关闭。
- 默认窗口置顶，适合边看视频边处理其他窗口。
- 支持透明度调节，范围为 30% 到 100%。
- 支持鼠标穿透，开启后鼠标点击会穿过播放器窗口，操作下面的应用。

### 2.2 网页打开与基础浏览

- 顶部地址栏可输入完整网址。
- 如果未输入 `http://` 或 `https://`，会自动补全为 `https://`。
- 支持后退、前进、刷新、主页。
- 默认主页为 `https://www.bilibili.com`，可在设置中修改。
- 外部网页弹窗会被拦截，并尽量在当前播放器内打开。

### 2.3 B 站播放增强

针对 B 站视频、番剧、列表等页面，提供两种布局模式：

- 纯净模式：突出播放器区域，隐藏大部分干扰内容。
- 选集模式：保留视频播放区域，同时尽量显示选集/分 P/剧集列表。

切换 B 站模式后，应用会自动向当前页面注入布局样式。如果 B 站页面结构变化导致无法识别播放器，会保留原页面并显示状态提示。

### 2.4 播放控制

工具栏和快捷键支持：

- 播放/暂停
- 后退 5 秒
- 前进 5 秒

这些控制依赖当前网页中可识别的 `video` 元素；如果页面没有可控制视频，会显示失败提示。

### 2.5 侧栏：标签、历史、收藏

左侧栏包含三个区域：

- 标签：保存的页面入口。
- 历史：最近访问过的页面。
- 收藏：手动收藏的页面。

侧栏支持搜索标题和网址。当前实现中，标签、历史、收藏各最多保留 10 条记录。

### 2.6 持久登录状态

应用使用 Electron 的持久化网页分区 `persist:amiyaplayer`。

这意味着：

- B 站等网站登录后，关闭并重新打开应用通常仍会保持登录。
- Cookie、localStorage、IndexedDB、缓存等由 Chromium 管理。
- 应用不会手动保存账号密码。

### 2.7 设置面板

设置入口在工具栏的设置按钮中。当前设置包括：

- 外观：透明度、主题色、侧栏宽度、播放模式是否显示顶栏。
- 启动：是否恢复标签列表、是否打开上次网址、主页地址。
- 工具栏：显示/隐藏按钮、修改按钮名称、设置快捷键、调整排序、删除按钮、恢复默认工具栏。
- 网页数据：清除当前站点数据、清除全部网页数据、打开用户数据目录。
- 重置全部设置：恢复默认设置，但不删除收藏和历史。

### 2.8 隐私与安全限制

当前程序对网页能力做了部分限制：

- 禁用或拦截 WebRTC 相关能力，降低本地 IP 泄露风险。
- 禁止网页请求屏幕捕获、地理位置、通知、媒体采集等敏感权限。
- 阻止部分 P2P、STUN、TURN、WebRTC 相关请求。
- 拦截新窗口弹窗，避免网页随意打开外部窗口。

## 3. 启动与打包

### 3.1 开发启动

在项目目录中执行：

```powershell
npm install
npm run start
```

### 3.2 检查代码质量与测试

```powershell
npm run lint
npm test
```

`npm test` 跑 Vitest，覆盖 `lib/` 下的纯函数（settings/items/URL/toolbar 排序）。

### 3.3 构建便携版

```powershell
npm run build
```

构建产物会输出到 `dist/`，文件名形如 `AemeathTool <version>.exe`。`productName` 已配置为 `AemeathTool`，对应 Windows 任务管理器中的显示名称。

## 4. 快速上手

1. 启动 AemeathTool。
2. 在顶部地址栏输入网址，例如 `https://www.bilibili.com`。
3. 点击“打开”或按回车进入页面。
4. 登录 B 站或其他网站；登录状态会随应用数据保留。
5. 打开视频页后，可使用工具栏切换 B 站纯净/选集布局。
6. 需要悬浮观看时，保持置顶开启，并按需调节透明度。
7. 需要操作窗口下方应用时，开启鼠标穿透；恢复操作播放器时按 `F8` 关闭穿透。

## 5. 工具栏按钮说明

默认工具栏包含：

| 功能 | 说明 | 默认快捷键 |
| --- | --- | --- |
| 后退 | 返回上一页 | `Alt+Left` |
| 前进 | 前往下一页 | `Alt+Right` |
| 刷新 | 刷新当前网页 | `F5` |
| 主页 | 打开设置中的主页 | 无 |
| 侧栏 | 显示或隐藏左侧栏 | 无 |
| 收藏当前页 | 将当前页面加入收藏 | 无 |
| 置顶 | 开关窗口置顶 | 无 |
| 鼠标穿透 | 开关鼠标穿透 | `F8` |
| 播放模式 | 隐藏侧栏，保留播放视图 | 无 |
| 纯净模式 | 隐藏顶部和侧栏，仅显示网页内容 | 无 |
| B 站纯净/选集 | 切换 B 站页面布局 | `F4` |
| 播放/暂停 | 控制网页中的视频 | `F9` |
| 后退 5 秒 | 视频后退 5 秒，默认隐藏 | `F10` |
| 前进 5 秒 | 视频前进 5 秒，默认隐藏 | `F11` |
| 降低透明度 | 每次降低 5%，默认隐藏 | `F6` |
| 提高透明度 | 每次提高 5%，默认隐藏 | `F7` |
| 设置 | 打开设置面板 | 无 |

说明：工具栏按钮可在设置中显示、隐藏、改名、排序和修改快捷键。`F8` 会作为鼠标穿透的稳定应急开关优先注册。

## 6. 常用操作

### 6.1 保存当前页

点击侧栏顶部的“保存页”，当前页面会加入“标签”列表。再次保存同一网址会更新到列表顶部。

### 6.2 收藏当前页

点击工具栏的收藏按钮，当前页面会加入“收藏”。收藏同一网址会更新标题和时间，不会重复创建多条。

### 6.3 搜索历史或收藏

在侧栏搜索框输入关键词，可以按页面标题或网址过滤当前列表。

### 6.4 切换播放视图

- 播放模式：隐藏侧栏，让网页区域更大。
- 纯净模式：隐藏顶栏和侧栏，只保留网页内容。
- 鼠标穿透：隐藏/弱化界面并让鼠标点击穿过窗口。

### 6.5 清理登录和缓存

进入设置的“网页数据”区域：

- 清除当前站点数据：只清除当前网址对应站点的登录、缓存等。
- 清除全部网页数据：清空所有网站的登录、缓存和站点数据。
- 打开用户数据目录：打开 Electron 存放设置、Cookie、缓存等数据的位置。

## 7. 数据保存位置

应用数据保存在 Electron 的 `userData` 目录中。可在设置中点击“打开用户数据目录”查看。

主要 JSON 文件：

- `settings.json`：设置、主页、透明度、工具栏配置、上次网址等。
- `tabs.json`：保存页。
- `history.json`：历史记录。
- `favorites.json`：收藏。

网页登录态和缓存由 Chromium 存放在同一用户数据目录下，不在这些 JSON 文件中。

## 8. 常见问题

### 开启鼠标穿透后点不到播放器怎么办？

按 `F8` 关闭鼠标穿透。`F8` 是默认应急快捷键。

### B 站纯净模式没有生效怎么办？

先确认当前页面是 B 站视频、番剧或列表播放页。页面加载完成后再切换一次 B 站模式。如果仍失败，通常是 B 站页面结构变化，应用会保留原页面显示。

### 播放/暂停快捷键没反应怎么办？

当前网页必须有可识别的视频元素。部分网站使用特殊播放器或跨域隔离时，应用可能无法直接控制播放。

### 为什么清除全部网页数据后需要重新登录？

因为这个操作会清空所有站点 Cookie、缓存和本地存储，相当于删除所有网页登录状态。

### 修改工具栏快捷键后没有生效怎么办？

检查设置面板中的快捷键提示。如果快捷键冲突或格式无效，应用会跳过注册。建议使用类似 `F9`、`Ctrl+Alt+P` 的格式。

## 9. 当前限制

- 标签、历史、收藏目前各最多保存 10 条（Wave 3 后会放大到 50 / 200）。
- 主要针对 B 站做了布局优化，其他视频网站按普通网页方式打开。
- 不是完整浏览器，没有多标签页实时切换、扩展插件、下载管理等完整浏览器功能。
- 网页内容运行在 webview 中，某些网站的反自动化或安全策略可能限制播放控制。
- 数据存储内部仍使用历史标识符 `persist:amiyaplayer`（Electron session 分区）；重命名为 AemeathTool 后未变，以保留已登录账号。

## 10. 近期改动（Wave 1，2026-05-15）

完成的修复与改进：

- **A2** 历史记录去重：每次导航只写一次 `history.json`，避免脏 title 短暂落盘。
- **A3** 透明度滑块拖动期间不再每像素落盘，松手后才写。
- **A6** Content-Security-Policy 收紧：移除对 `<webview>` 无效的 `frame-src`，新增 `connect-src 'self'`。
- **A7** 全局快捷键改为差异化注册：改设置不再粗暴 `unregisterAll`，F8 应急穿透稳定保留。
- **B5** Chromium 启动开关全部加注释，集中在一个数组里管理。
- 工程化：接入 **ESLint 9** + **Vitest 1**，新增 4 个测试文件覆盖 normalize 系列函数（27 个用例全部通过）。
- 品牌：窗口标题、顶栏品牌名、`productName` 全部更新为 **AemeathTool**；顶栏徽标改用 `icon.ico`。

未完成（后续 Wave 2–4 推进）：架构重构（preload 桥 + 文件拆分）、Electron 28→33、Electron 安全配置收紧、上限放大、应急 UI 锚点、托盘 + Boss Key、命令面板、数据导入导出、CI。详情见 `docs/superpowers/specs/2026-05-15-amiyaplayer-optimization-design.md`。

